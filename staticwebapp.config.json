{
  "routes": [
    {
      "route": "/*",
      "allowedRoles": ["authenticated"],
      "statusCode": 302,
      "redirect": "/.auth/login/aad"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html"
  }
}
